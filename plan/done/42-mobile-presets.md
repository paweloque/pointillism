# Task 42 — Mobile: replace sliders with presets

**Phase**: 4 (Parameter playground)
**Spec**: `specs/ux-progressive-enhancement.md`

## Problem

The mobile bottom sheet exposed density and size sliders — fiddly controls that don't showcase the app's interesting effects. The close handle bar was also too subtle.

## Deliverables

- Replace density/size sliders with style presets (subtle/dense/dreamy/energy) and motion presets (off/subtle/animated)
- Keep background color swatches, upload, and export buttons
- Replace abstract handle bar with visible "✕ close" text button
- Update the UX spec to reflect presets-first mobile strategy

## Acceptance

- Bottom sheet shows preset buttons instead of sliders
- Tapping a preset updates the dot field
- Close button is clearly visible and dismisses the sheet
- Desktop sidebar unchanged
- Build passes, tests pass
